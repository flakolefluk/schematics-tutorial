{
  "$schema": "http://json-schema.org/schema",
  "id": "my-collection-update-recorder",
  "title": "Uses recorder options",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path of the file to record update.",
      "x-prompt":"Enter the file path:"
    }
  },
  "required": ["path"]
}
